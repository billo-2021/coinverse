import { AuthenticationConfiguration } from "../../models/authentication-configuration.model";
import { config as defaultCofig } from './default.config';

export const config: AuthenticationConfiguration = {
    ...defaultCofig
}